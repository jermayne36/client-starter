# Hoist @prisma packages to the workspace root so they resolve correctly
# across all packages. Without this, @prisma/client is isolated inside
# packages/database/node_modules and can't be found from apps/web at runtime.
public-hoist-pattern[]=@prisma/*
public-hoist-pattern[]=.prisma
