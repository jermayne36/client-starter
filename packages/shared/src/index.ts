// Shared types, constants, and Zod schemas for the monorepo
// Add exports here as the project grows

export type { AppConfig } from "./types";
export { APP_NAME, DEFAULT_LOCALE } from "./constants";
